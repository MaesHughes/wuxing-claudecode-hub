# /commit

Smart git commit command that helps you write better commit messages.

## Usage

```
/commit
```

This command will:
1. Check git status for staged changes
2. Prompt you for a commit message
3. Create a conventional commit message
4. Execute the commit

## Features

- Automatic git status check
- Conventional commit format
- Commit message validation
- Easy rollback if needed
